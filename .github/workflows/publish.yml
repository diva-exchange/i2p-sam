name: JSR Publish

on:
  push:
    branches:
      - develop-deno-i2p-sam

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - run: npx jsr publish
